__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# 唯一真相源，不入库版本控制
data/*.db
data/*.db-journal
data/*.db.bak_*
data/logs/*.log

# 真实凭证
.env

!data/.gitkeep
!data/logs/.gitkeep

# Cursor Background Agent 沙箱 cwd（src/analysis/cursor_agent.py），agent 运行时
# 可能在这里写临时文件，内容不重要、不入库
data/.cursor_agent_sandbox/*
